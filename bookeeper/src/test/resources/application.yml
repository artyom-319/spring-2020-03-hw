spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode : always
    schema: schema.sql
    data: data.sql

  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: false
    show-sql: true

  shell:
    interactive:
      enabled: false
